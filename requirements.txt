# Streamlit Community Cloud installs this file at deploy time.
# Installing "." pulls in the dji_waypoints package and all its
# runtime deps from pyproject.toml (djikmz, pyshp, fastkml, shapely,
# pyproj, lxml).
.
streamlit>=1.42

# Legacy stand-alone scripts at the repo root (not used by the web app)
python-docx
